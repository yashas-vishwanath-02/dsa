class Solution {
public:
    void reverseString(vector<char>& s) {
        int n = s.size();
        int i = s[0];
        int j = s[n - 1];
        while(i < j){
            swap(s[i], s[j]);
            i++;
            j--;
        }
        
    }
};
